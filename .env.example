SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-key

# Optional. Leave empty on Vercel when frontend and API are deployed together.
# Use this only when the API is hosted on another domain.
VITE_API_BASE_URL=
